schema{
    query:Query
}

type Query{
    allSatellites: [Satellite]
    satelliteByNumber(satelliteNumber: Int): Satellite
    satelliteById(id: ID): Satellite
}

type Satellite{
    id:ID
    satelliteNumber:Int
    name:String
    intlDesignator:String
    category:Int
}
